
//O(nlog(n) time| O(1) space0 average,worst and best case
const heapSort = array => {


   


    return array;

};


const swap = (i, j, array) => {
    let temp = array[j];
    array[j] = array[i];
    array[i] = temp;
}

module.exports = heapSort;